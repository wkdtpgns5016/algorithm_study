// const fs = require('fs');
// const input = fs.readFileSync('/dev/stdin').toString().trim();

const input = ["100", "2"];
const m = parseInt(input[0]);
const n = parseInt(input[1]);

console.log(m*n);